# Copia este archivo a `.env` y ajusta los valores.
# El backend lee estas variables de entorno automáticamente (dotenv).

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=tu_password_aqui
DB_NAME=softskills
DB_SCHEMA=habilidades

DEBUG=false